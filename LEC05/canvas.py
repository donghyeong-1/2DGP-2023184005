## 여기를 채우시오.
from pico2d import * ## pico2d의 모든 함수를 가지고 와라
## 그냥 import를 하면 해당 함수를 이용하기 위해서 pico2d.함수명()을 이용해야 한다.

open_canvas(800, 600) ## 캔버스의 크기를 800x600으로 설정하고 열어라
update_canvas() ## 캔버스에 그려라
delay(5) ## 5초 동안 대기하라
close_canvas() ## 캔버스를 닫아라